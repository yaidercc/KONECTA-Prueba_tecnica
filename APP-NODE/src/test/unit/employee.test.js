/**
 * Verificar que el registro de usuarios funciona correctamente
 * Validar que el rol exista
 * Debe devolver null para un ID de empleado no existente
 * Validar que se obtiene un empleado existente
 */

describe('Pruebas en el modulo de empleados', () => { 
    })